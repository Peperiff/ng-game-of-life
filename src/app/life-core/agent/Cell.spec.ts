import { Cell } from './Cell';

describe('Cell', () => {
  beforeEach(() => {
    // nothing yet
  });

  it('should create Cell', () => {
    const grid: Cell = new Cell();
    expect(grid).toBeTruthy();
  });
});
